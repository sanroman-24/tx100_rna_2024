Pending: 
- Include script to calculate the CN distance between samples or just provide as additional data?
- Add library(here) in most of the scripts
- Check that plot dir is always PLOT_DIR rather than OUT_DIR
- Check plots that I did not save using function save_baseplot
- Check that all the summarise functions have dplyr:: before

Notes:
- sample_annotation in meta folder replaces the previous sample_short.tsv

Order to run scripts:
- Scripts with numbers thus far could go into a folder called preprocessing. This should be run first
- Then run generate_oncoprint.R
- Then run umap.R
- Then run run_pca.R and lme_transcriptional_pc.R (in that order, make that obvious to reader)
- Then run ....